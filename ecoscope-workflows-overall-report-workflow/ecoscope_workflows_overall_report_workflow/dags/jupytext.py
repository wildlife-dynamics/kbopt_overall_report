# AUTOGENERATED BY ECOSCOPE-WORKFLOWS; see fingerprint in README.md for details


# ruff: noqa: E402

# %% [markdown]
# # Overall Report
# TODO: top level description

# %% [markdown]
# ## Imports

import os

from ecoscope_workflows_core.tasks.config import set_string_var as set_string_var
from ecoscope_workflows_core.tasks.config import (
    set_workflow_details as set_workflow_details,
)
from ecoscope_workflows_core.tasks.filter import (
    get_timezone_from_time_range as get_timezone_from_time_range,
)
from ecoscope_workflows_core.tasks.filter import set_time_range as set_time_range
from ecoscope_workflows_core.tasks.groupby import groupbykey as groupbykey
from ecoscope_workflows_core.tasks.groupby import set_groupers as set_groupers
from ecoscope_workflows_core.tasks.groupby import split_groups as split_groups
from ecoscope_workflows_core.tasks.io import persist_text as persist_text
from ecoscope_workflows_core.tasks.io import set_er_connection as set_er_connection
from ecoscope_workflows_core.tasks.results import gather_dashboard as gather_dashboard
from ecoscope_workflows_core.tasks.skip import (
    any_dependency_skipped as any_dependency_skipped,
)
from ecoscope_workflows_core.tasks.skip import any_is_empty_df as any_is_empty_df
from ecoscope_workflows_core.tasks.skip import never as never
from ecoscope_workflows_core.tasks.transformation import (
    add_temporal_index as add_temporal_index,
)
from ecoscope_workflows_core.tasks.transformation import (
    convert_values_to_timezone as convert_values_to_timezone,
)
from ecoscope_workflows_core.tasks.transformation import map_columns as map_columns
from ecoscope_workflows_core.tasks.transformation import sort_values as sort_values
from ecoscope_workflows_ext_big_life.tasks import aggregate_by as aggregate_by
from ecoscope_workflows_ext_big_life.tasks import convert_to_string as convert_to_string
from ecoscope_workflows_ext_big_life.tasks import get_color_map as get_color_map
from ecoscope_workflows_ext_big_life.tasks import (
    map_color_column_value as map_color_column_value,
)
from ecoscope_workflows_ext_big_life.tasks import map_column_value as map_column_value
from ecoscope_workflows_ext_custom.tasks.io import html_to_png as html_to_png
from ecoscope_workflows_ext_custom.tasks.io import load_df as load_df
from ecoscope_workflows_ext_custom.tasks.io import (
    persist_df_wrapper as persist_df_wrapper,
)
from ecoscope_workflows_ext_custom.tasks.io import (
    process_events_details as process_events_details,
)
from ecoscope_workflows_ext_custom.tasks.results import (
    create_geojson_layer as create_geojson_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import (
    create_path_layer as create_path_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import (
    create_scatterplot_layer as create_scatterplot_layer,
)
from ecoscope_workflows_ext_custom.tasks.results import draw_map as draw_map
from ecoscope_workflows_ext_custom.tasks.results import (
    set_base_maps_pydeck as set_base_maps_pydeck,
)
from ecoscope_workflows_ext_custom.tasks.spatial_ops import (
    reproject_gdf as reproject_gdf,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    drop_column_prefix as drop_column_prefix,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    drop_null_geometry as drop_null_geometry,
)
from ecoscope_workflows_ext_custom.tasks.transformation import (
    filter_row_values as filter_row_values,
)
from ecoscope_workflows_ext_ecoscope.tasks.analysis import summarize_df as summarize_df
from ecoscope_workflows_ext_ecoscope.tasks.io import get_events as get_events
from ecoscope_workflows_ext_ecoscope.tasks.io import (
    get_patrol_observations_from_patrols_df_and_combined_params as get_patrol_observations_from_patrols_df_and_combined_params,
)
from ecoscope_workflows_ext_ecoscope.tasks.io import (
    get_patrols_from_combined_params as get_patrols_from_combined_params,
)
from ecoscope_workflows_ext_ecoscope.tasks.io import (
    set_patrols_and_patrol_events_params as set_patrols_and_patrol_events_params,
)
from ecoscope_workflows_ext_ecoscope.tasks.preprocessing import (
    process_relocations as process_relocations,
)
from ecoscope_workflows_ext_ecoscope.tasks.preprocessing import (
    relocations_to_trajectory as relocations_to_trajectory,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    add_spatial_index as add_spatial_index,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    apply_color_map as apply_color_map,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    normalize_json_column as normalize_json_column,
)
from ecoscope_workflows_ext_ecoscope.tasks.transformation import (
    resolve_spatial_feature_groups_for_spatial_groupers as resolve_spatial_feature_groups_for_spatial_groupers,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    add_column_from_index_level as add_column_from_index_level,
)
from ecoscope_workflows_ext_kbopt.tasks import apply_bin_colors as apply_bin_colors
from ecoscope_workflows_ext_kbopt.tasks import bin_with_unvisited as bin_with_unvisited
from ecoscope_workflows_ext_kbopt.tasks import (
    build_inactive_nests_table as build_inactive_nests_table,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    build_nest_summary_table as build_nest_summary_table,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    compute_nest_cycle_status as compute_nest_cycle_status,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    convert_kbopt_to_regions_gdf as convert_kbopt_to_regions_gdf,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    custom_combine_deckgl_map_layers as custom_combine_deckgl_map_layers,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    custom_map_color_column_value as custom_map_color_column_value,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    filter_by_time_range as filter_by_time_range,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    generate_nest_report as generate_nest_report,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    get_active_nests_table as get_active_nests_table,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    get_first_unique_str as get_first_unique_str,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    get_focal_species_df as get_focal_species_df,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    get_inactive_nests_table as get_inactive_nests_table,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    get_time_range_before_since as get_time_range_before_since,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    get_time_range_to_until as get_time_range_to_until,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    inject_logo_into_html as inject_logo_into_html,
)
from ecoscope_workflows_ext_kbopt.tasks import merge_names as merge_names
from ecoscope_workflows_ext_kbopt.tasks import parse_df_point as parse_df_point
from ecoscope_workflows_ext_kbopt.tasks import (
    patrol_coverage_grid as patrol_coverage_grid,
)
from ecoscope_workflows_ext_kbopt.tasks import (
    resolve_duplicate_nests_by_outcome as resolve_duplicate_nests_by_outcome,
)
from ecoscope_workflows_ext_kbopt.tasks import sort_by_precedence as sort_by_precedence
from ecoscope_workflows_ext_mnc.tasks import (
    replace_missing_with_label as replace_missing_with_label,
)
from ecoscope_workflows_ext_ste.tasks import (
    create_deckgl_layer_from_gdf as create_deckgl_layer_from_gdf,
)
from ecoscope_workflows_ext_ste.tasks import (
    fetch_and_persist_file as fetch_and_persist_file,
)
from ecoscope_workflows_ext_ste.tasks import get_gdf_geom_type as get_gdf_geom_type
from ecoscope_workflows_ext_ste.tasks import view_state_deck_gdf as view_state_deck_gdf
from ecoscope_workflows_ext_ste.tasks import zip_groupbykey as zip_groupbykey

# %% [markdown]
# ## Set Workflow Details

# %%
# parameters

workflow_details_params = dict(
    name=...,
    description=...,
    image_url=...,
)

# %%
# call the task


workflow_details = (
    set_workflow_details.set_task_instance_id("workflow_details")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**workflow_details_params)
    .call()
)


# %% [markdown]
# ## Time range

# %%
# parameters

time_range_params = dict(
    since=...,
    until=...,
    timezone=...,
    time_format=...,
)

# %%
# call the task


time_range = (
    set_time_range.set_task_instance_id("time_range")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**time_range_params)
    .call()
)


# %% [markdown]
# ## Extract Timezone Selection

# %%
# parameters

get_timezone_params = dict()

# %%
# call the task


get_timezone = (
    get_timezone_from_time_range.set_task_instance_id("get_timezone")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(time_range=time_range, **get_timezone_params)
    .call()
)


# %% [markdown]
# ## Set groupers

# %%
# parameters

groupers_params = dict()

# %%
# call the task


groupers = (
    set_groupers.set_task_instance_id("groupers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(groupers=[{"spatial_index_name": "Conservation Name"}], **groupers_params)
    .call()
)


# %% [markdown]
# ## Connect to ER

# %%
# parameters

er_client_name_params = dict(
    data_source=...,
)

# %%
# call the task


er_client_name = (
    set_er_connection.set_task_instance_id("er_client_name")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**er_client_name_params)
    .call()
)


# %% [markdown]
# ## Base Maps

# %%
# parameters

configure_base_maps_params = dict()

# %%
# call the task


configure_base_maps = (
    set_base_maps_pydeck.set_task_instance_id("configure_base_maps")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        base_maps=[
            {
                "url": "https://server.arcgisonline.com/arcgis/rest/services/Elevation/World_Hillshade/MapServer/tile/{z}/{y}/{x}",
                "opacity": 1,
                "max_zoom": 20,
            },
            {
                "url": "https://server.arcgisonline.com/ArcGIS/rest/services/World_Street_Map/MapServer/tile/{z}/{y}/{x}",
                "opacity": 0.04,
                "max_zoom": 20,
            },
        ],
        **configure_base_maps_params,
    )
    .call()
)


# %% [markdown]
# ## Set time range from 2000-01-01 to until

# %%
# parameters

overall_time_range_params = dict()

# %%
# call the task


overall_time_range = (
    get_time_range_to_until.set_task_instance_id("overall_time_range")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(time_range=time_range, **overall_time_range_params)
    .call()
)


# %% [markdown]
# ## Get time range from 2000-01-01 to since in time range

# %%
# parameters

previous_time_range_params = dict()

# %%
# call the task


previous_time_range = (
    get_time_range_before_since.set_task_instance_id("previous_time_range")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(time_range=time_range, **previous_time_range_params)
    .call()
)


# %% [markdown]
# ## Download KBOPT geoparquet file

# %%
# parameters

persist_kbopt_gpq_params = dict()

# %%
# call the task


persist_kbopt_gpq = (
    fetch_and_persist_file.set_task_instance_id("persist_kbopt_gpq")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/i476ltynyt7zimi8zb8f2/kbopt_.geoparquet?rlkey=50dmzukcqj2x5kzqjj7eyc8no&st=bfawmyer&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_kbopt_gpq_params,
    )
    .call()
)


# %% [markdown]
# ## Download KBOPT logo image for html

# %%
# parameters

persist_kbopt_logo_params = dict()

# %%
# call the task


persist_kbopt_logo = (
    fetch_and_persist_file.set_task_instance_id("persist_kbopt_logo")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/1f2z6er8f06jj9rrhlac2/kbopt?rlkey=enpczqy88e1uidag1m4bqmske&st=gzdsj2lp&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_kbopt_logo_params,
    )
    .call()
)


# %% [markdown]
# ## Load kbopt geoparquet file

# %%
# parameters

load_kbopt_gpq_params = dict()

# %%
# call the task


load_kbopt_gpq = (
    load_df.set_task_instance_id("load_kbopt_gpq")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        file_path=persist_kbopt_gpq,
        layer=None,
        deserialize_json=False,
        **load_kbopt_gpq_params,
    )
    .call()
)


# %% [markdown]
# ## Reproject gdf to 4326 for kbopt boundaries

# %%
# parameters

reproject_kbopt_gpq_params = dict()

# %%
# call the task


reproject_kbopt_gpq = (
    reproject_gdf.set_task_instance_id("reproject_kbopt_gpq")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(gdf=load_kbopt_gpq, target_crs="EPSG:4326", **reproject_kbopt_gpq_params)
    .call()
)


# %% [markdown]
# ## Filter loaded kbopt gdf by AOI

# %%
# parameters

filter_kbopt_aoi_params = dict()

# %%
# call the task


filter_kbopt_aoi = (
    filter_row_values.set_task_instance_id("filter_kbopt_aoi")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=reproject_kbopt_gpq,
        column="Conservation Name",
        values=["Mara Triangle", "Naboisho", "Maasai Mara National Reserve"],
        **filter_kbopt_aoi_params,
    )
    .call()
)


# %% [markdown]
# ## Convert kbopt GDF to regions format

# %%
# parameters

kbopt_regions_gdf_params = dict()

# %%
# call the task


kbopt_regions_gdf = (
    convert_kbopt_to_regions_gdf.set_task_instance_id("kbopt_regions_gdf")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=filter_kbopt_aoi,
        name_column="Conservation Name",
        **kbopt_regions_gdf_params,
    )
    .call()
)


# %% [markdown]
# ## Resolve spatial groupers with kbopt regions

# %%
# parameters

resolved_groupers_params = dict()

# %%
# call the task


resolved_groupers = (
    resolve_spatial_feature_groups_for_spatial_groupers.set_task_instance_id(
        "resolved_groupers"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            never,
        ],
        unpack_depth=1,
    )
    .partial(
        groupers=groupers,
        spatial_feature_groups=kbopt_regions_gdf,
        **resolved_groupers_params,
    )
    .call()
)


# %% [markdown]
# ## Retrieve all nest id events to date

# %%
# parameters

retrieve_nest_ids_params = dict()

# %%
# call the task


retrieve_nest_ids = (
    get_events.set_task_instance_id("retrieve_nest_ids")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        client=er_client_name,
        time_range=overall_time_range,
        event_columns=[
            "id",
            "time",
            "event_type",
            "event_category",
            "reported_by",
            "serial_number",
            "geometry",
            "created_at",
            "event_details",
        ],
        event_types=["nest_id"],
        raise_on_empty=False,
        include_details=True,
        include_updates=False,
        include_related_events=False,
        include_null_geometry=False,
        include_display_values=False,
        **retrieve_nest_ids_params,
    )
    .call()
)


# %% [markdown]
# ## Retrieve all nest checks before hand until since

# %%
# parameters

retrieve_previous_nest_events_params = dict()

# %%
# call the task


retrieve_previous_nest_events = (
    get_events.set_task_instance_id("retrieve_previous_nest_events")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        client=er_client_name,
        time_range=previous_time_range,
        event_columns=[
            "id",
            "time",
            "event_type",
            "event_category",
            "reported_by",
            "serial_number",
            "geometry",
            "created_at",
            "event_details",
        ],
        event_types=["nest_check"],
        raise_on_empty=False,
        include_details=True,
        include_updates=False,
        include_related_events=False,
        include_null_geometry=False,
        include_display_values=False,
        **retrieve_previous_nest_events_params,
    )
    .call()
)


# %% [markdown]
# ## Retrieve nest check events

# %%
# parameters

get_events_data_params = dict()

# %%
# call the task


get_events_data = (
    get_events.set_task_instance_id("get_events_data")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        client=er_client_name,
        time_range=time_range,
        event_columns=[
            "id",
            "time",
            "event_type",
            "event_category",
            "reported_by",
            "serial_number",
            "geometry",
            "created_at",
            "event_details",
        ],
        event_types=["nest_check"],
        raise_on_empty=False,
        include_details=True,
        include_updates=False,
        include_related_events=False,
        include_null_geometry=False,
        include_display_values=False,
        **get_events_data_params,
    )
    .call()
)


# %% [markdown]
# ## Process current nest_check events details

# %%
# parameters

process_current_nest_check_params = dict()

# %%
# call the task


process_current_nest_check = (
    process_events_details.set_task_instance_id("process_current_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=get_events_data,
        client=er_client_name,
        map_to_titles=True,
        ordered=True,
        **process_current_nest_check_params,
    )
    .call()
)


# %% [markdown]
# ## Normalize nest_check events details

# %%
# parameters

normalize_nest_check_params = dict()

# %%
# call the task


normalize_nest_check = (
    normalize_json_column.set_task_instance_id("normalize_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column="event_details",
        df=process_current_nest_check,
        skip_if_not_exists=True,
        sort_columns=True,
        **normalize_nest_check_params,
    )
    .call()
)


# %% [markdown]
# ## Drop column prefix from normalized nest_check details

# %%
# parameters

drop_nest_check_prefix_params = dict()

# %%
# call the task


drop_nest_check_prefix = (
    drop_column_prefix.set_task_instance_id("drop_nest_check_prefix")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=normalize_nest_check,
        prefix="event_details__",
        duplicate_strategy="keep_original",
        **drop_nest_check_prefix_params,
    )
    .call()
)


# %% [markdown]
# ## Process nest_id events details

# %%
# parameters

process_nest_id_params = dict()

# %%
# call the task


process_nest_id = (
    process_events_details.set_task_instance_id("process_nest_id")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=retrieve_nest_ids,
        client=er_client_name,
        map_to_titles=True,
        ordered=True,
        **process_nest_id_params,
    )
    .call()
)


# %% [markdown]
# ## Normalize nest_id events details

# %%
# parameters

normalize_nest_id_params = dict()

# %%
# call the task


normalize_nest_id = (
    normalize_json_column.set_task_instance_id("normalize_nest_id")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column="event_details",
        df=process_nest_id,
        skip_if_not_exists=True,
        sort_columns=True,
        **normalize_nest_id_params,
    )
    .call()
)


# %% [markdown]
# ## Drop column prefix from normalized nest_id details

# %%
# parameters

drop_nest_id_prefix_params = dict()

# %%
# call the task


drop_nest_id_prefix = (
    drop_column_prefix.set_task_instance_id("drop_nest_id_prefix")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=normalize_nest_id,
        prefix="event_details__",
        duplicate_strategy="keep_original",
        **drop_nest_id_prefix_params,
    )
    .call()
)


# %% [markdown]
# ## Process previous nest_check events details

# %%
# parameters

process_previous_nest_check_params = dict()

# %%
# call the task


process_previous_nest_check = (
    process_events_details.set_task_instance_id("process_previous_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=retrieve_previous_nest_events,
        client=er_client_name,
        map_to_titles=True,
        ordered=True,
        **process_previous_nest_check_params,
    )
    .call()
)


# %% [markdown]
# ## Normalize previous nest_check events details

# %%
# parameters

normalize_previous_nest_check_params = dict()

# %%
# call the task


normalize_previous_nest_check = (
    normalize_json_column.set_task_instance_id("normalize_previous_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column="event_details",
        df=process_previous_nest_check,
        skip_if_not_exists=True,
        sort_columns=True,
        **normalize_previous_nest_check_params,
    )
    .call()
)


# %% [markdown]
# ## Drop column prefix from previous normalized nest_check details

# %%
# parameters

drop_previous_nest_check_prefix_params = dict()

# %%
# call the task


drop_previous_nest_check_prefix = (
    drop_column_prefix.set_task_instance_id("drop_previous_nest_check_prefix")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=normalize_previous_nest_check,
        prefix="event_details__",
        duplicate_strategy="keep_original",
        **drop_previous_nest_check_prefix_params,
    )
    .call()
)


# %% [markdown]
# ## Add spatial index to current nest_check events

# %%
# parameters

add_spatial_idx_curr_nest_chk_params = dict()

# %%
# call the task


add_spatial_idx_curr_nest_chk = (
    add_spatial_index.set_task_instance_id("add_spatial_idx_curr_nest_chk")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=drop_nest_check_prefix,
        groupers=resolved_groupers,
        **add_spatial_idx_curr_nest_chk_params,
    )
    .call()
)


# %% [markdown]
# ## Restore Conservation Name column for current nest_check events

# %%
# parameters

restore_current_col_nest_check_params = dict()

# %%
# call the task


restore_current_col_nest_check = (
    add_column_from_index_level.set_task_instance_id("restore_current_col_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=add_spatial_idx_curr_nest_chk,
        index_name="SpatialGrouper_Conservation Name",
        column_name="Conservation Name",
        **restore_current_col_nest_check_params,
    )
    .call()
)


# %% [markdown]
# ## Add spatial index to previous nest_check events

# %%
# parameters

add_spatial_idx_prev_nest_chk_params = dict()

# %%
# call the task


add_spatial_idx_prev_nest_chk = (
    add_spatial_index.set_task_instance_id("add_spatial_idx_prev_nest_chk")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=drop_previous_nest_check_prefix,
        groupers=resolved_groupers,
        **add_spatial_idx_prev_nest_chk_params,
    )
    .call()
)


# %% [markdown]
# ## Restore Conservation Name column for previous nest_check events

# %%
# parameters

restore_previous_col_nest_check_params = dict()

# %%
# call the task


restore_previous_col_nest_check = (
    add_column_from_index_level.set_task_instance_id("restore_previous_col_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=add_spatial_idx_prev_nest_chk,
        index_name="SpatialGrouper_Conservation Name",
        column_name="Conservation Name",
        **restore_previous_col_nest_check_params,
    )
    .call()
)


# %% [markdown]
# ## Add spatial index to nest_id events

# %%
# parameters

add_spatial_index_nest_id_params = dict()

# %%
# call the task


add_spatial_index_nest_id = (
    add_spatial_index.set_task_instance_id("add_spatial_index_nest_id")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=drop_nest_id_prefix,
        groupers=resolved_groupers,
        **add_spatial_index_nest_id_params,
    )
    .call()
)


# %% [markdown]
# ## Restore Conservation Name column for nest_id events

# %%
# parameters

restore_col_nest_id_params = dict()

# %%
# call the task


restore_col_nest_id = (
    add_column_from_index_level.set_task_instance_id("restore_col_nest_id")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=add_spatial_index_nest_id,
        index_name="SpatialGrouper_Conservation Name",
        column_name="Conservation Name",
        **restore_col_nest_id_params,
    )
    .call()
)


# %% [markdown]
# ## Replace Unknown Values in current nest_check events

# %%
# parameters

replace_current_missing_unknown_params = dict()

# %%
# call the task


replace_current_missing_unknown = (
    replace_missing_with_label.set_task_instance_id("replace_current_missing_unknown")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=restore_current_col_nest_check,
        columns=["Species"],
        label="Unknown",
        **replace_current_missing_unknown_params,
    )
    .call()
)


# %% [markdown]
# ## Convert current nest_check Species to string

# %%
# parameters

convert_current_species_string_params = dict()

# %%
# call the task


convert_current_species_string = (
    convert_to_string.set_task_instance_id("convert_current_species_string")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        columns=["Species"],
        inplace=False,
        df=replace_current_missing_unknown,
        **convert_current_species_string_params,
    )
    .call()
)


# %% [markdown]
# ## Replace Unknown Values in previous nest_check events

# %%
# parameters

replace_previous_missing_unknown_params = dict()

# %%
# call the task


replace_previous_missing_unknown = (
    replace_missing_with_label.set_task_instance_id("replace_previous_missing_unknown")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=restore_previous_col_nest_check,
        columns=["Species"],
        label="Unknown",
        **replace_previous_missing_unknown_params,
    )
    .call()
)


# %% [markdown]
# ## Convert previous nest_check Species to string

# %%
# parameters

convert_previous_species_string_params = dict()

# %%
# call the task


convert_previous_species_string = (
    convert_to_string.set_task_instance_id("convert_previous_species_string")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        columns=["Species"],
        inplace=False,
        df=replace_previous_missing_unknown,
        **convert_previous_species_string_params,
    )
    .call()
)


# %% [markdown]
# ## Map species columns

# %%
# parameters

map_current_species_column_params = dict()

# %%
# call the task


map_current_species_column = (
    map_column_value.set_task_instance_id("map_current_species_column")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=convert_current_species_string,
        col="Species",
        new_col="Species",
        mapping={
            "Bateleur": "Bateleur",
            "Buzzard_Augur": "Augur Buzzard",
            "Buzzard_Lizard": "Lizard Buzzard",
            "Chanting-goshawk_Dark": "Dark chanting goshawk",
            "Crane_Grey-crowned": "Grey crowned crane",
            "Cuckoo-hawk_African": "African cuckoo-hawk",
            "Eagle_Crowned": "Crowned eagle",
            "Eagle_Long-crested": "Long-crested eagle",
            "Eagle_Martial": "Martial eagle",
            "Eagle_Tawny": "Tawny eagle",
            "Eagle_Verreaux's": "Verreaux's eagle",
            "Eagle_Wahlberg's": "Wahlberg's eagle",
            "Eagle-owl_Spotted": "Spotted eagle owl",
            "Eagle-owl_Verreaux's": "Verreaux's eagle owl",
            "Falcon_Lanner": "Lanner falcon",
            "Falcon_Peregrine": "Peregrine falcon",
            "Fish-eagle_African": "African fish eagle",
            "Fishing-owl_Pel's": "Pel's fishing owl",
            "Goose_Egyptian": "Egyptian goose",
            "Goshawk_African": "African goshawk",
            "Goshawk_Gabar": "Gabar goshawk",
            "Harrier-hawk_African": "African harrier hawk",
            "Hawk_Bat": "Bat hawk",
            "Hawk-eagle_African": "African hawk eagle",
            "Hawk-eagle_Ayres's": "Ayre's hawk eagle",
            "Hobby_African": "African hobby",
            "Hornbill_Southern_Ground": "Southern ground hornbill",
            "Kestrel_Grey": "Grey kestrel",
            "Kestrel_Mountain": "Mountain kestrel",
            "Kite_Black": "Black kite",
            "Kite_Black-winged": "Black-winged kite",
            "Owl_African_Wood": "African wood owl",
            "Owl_Barn": "Barn owl",
            "Owl_Marsh": "Marsh owl",
            "Owl_Southern_White-faced": "Southern white-faced owl",
            "Owlet_Pearl-spotted": "Pearl-spotted owlet",
            "Pygmy-falcon_African": "African pygmy falcon",
            "Scops-owl_African": "African scops owl",
            "Secretarybird": "Secretarybird",
            "Shikra": "Shikra",
            "Snake-eagle_Black-chested": "Black-chested snake eagle",
            "Snake-eagle_Brown": "Brown snake eagle",
            "Snake-eagle_Western_Banded": "Western banded snake eagle",
            "Sparrowhawk_Black": "Black sparrowhawk",
            "Sparrowhawk_Little": "Little sparrowhawk",
            "Sparrowhawk_Ovambo": "Ovambo sparrowhawk",
            "Stork_Saddle-billed": "Saddle-billed stork",
            "Vulture_Hooded": "Hooded vulture",
            "Vulture_Lappet-faced": "Lappet-faced vulture",
            "Vulture_White-backed": "White-backed vulture",
            "Vulture_White-headed": "White-headed vulture",
            "Others": "Others",
            "Unknown": "Unknown",
        },
        **map_current_species_column_params,
    )
    .call()
)


# %% [markdown]
# ## Assign colors to species

# %%
# parameters

species_colors_params = dict()

# %%
# call the task


species_colors = (
    get_color_map.set_task_instance_id("species_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_current_species_column,
        col="Species",
        color_dict={
            "Bateleur": "#2E6045",
            "Augur Buzzard": "#D86DCD",
            "Lizard Buzzard": "#B5E6A2",
            "Dark chanting goshawk": "#56250A",
            "Grey crowned crane": "#72FCD1",
            "African cuckoo-hawk": "#FFFF99",
            "Crowned eagle": "#0AA281",
            "Long-crested eagle": "#A6C9EC",
            "Martial eagle": "#156082",
            "Tawny eagle": "#9B612D",
            "Verreaux's eagle": "#C5ABB1",
            "Wahlberg's eagle": "#F98C07",
            "Spotted eagle owl": "#3C7D22",
            "Verreaux's eagle owl": "#B81E04",
            "Lanner falcon": "#E0A762",
            "Peregrine falcon": "#E0A762",
            "African fish eagle": "#FFCC00",
            "Pel's fishing owl": "#3C7D22",
            "Egyptian goose": "#F3AC85",
            "African goshawk": "#3292DA",
            "Gabar goshawk": "#C0E6F5",
            "African harrier hawk": "#5CC836",
            "Bat hawk": "#2CE4DB",
            "African hawk eagle": "#2CE4DB",
            "Ayre's hawk eagle": "#2CE4DB",
            "African hobby": "#C0E6F5",
            "Southern ground hornbill": "#FF3333",
            "Grey kestrel": "#7F2376",
            "Mountain kestrel": "#7F2376",
            "Black kite": "#F0EA00",
            "Black-winged kite": "#F0EA00",
            "African wood owl": "#3C7D22",
            "Barn owl": "#3C7D22",
            "Marsh owl": "#3C7D22",
            "Southern white-faced owl": "#3C7D22",
            "Pearl-spotted owlet": "#3C7D22",
            "African pygmy falcon": "#E0A762",
            "African scops owl": "#3C7D22",
            "Secretarybird": "#338577",
            "Shikra": "#F7E1F5",
            "Black-chested snake eagle": "#808080",
            "Brown snake eagle": "#BF7823",
            "Western banded snake eagle": "#534C15",
            "Black sparrowhawk": "#0F9ED5",
            "Little sparrowhawk": "#0F9ED5",
            "Ovambo sparrowhawk": "#0F9ED5",
            "Saddle-billed stork": "#CD3B5E",
            "Hooded vulture": "#75DBFF",
            "Lappet-faced vulture": "#52349E",
            "White-backed vulture": "#DAD486",
            "White-headed vulture": "#F35757",
            "Others": "#FFFFFF",
            "Unknown": "#000000",
        },
        **species_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map species to colors for nest check events

# %%
# parameters

map_species_colors_params = dict()

# %%
# call the task


map_species_colors = (
    custom_map_color_column_value.set_task_instance_id("map_species_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_current_species_column,
        col="Species",
        mapping=species_colors,
        new_col="species_colors",
        default_color="#808080",
        **map_species_colors_params,
    )
    .call()
)


# %% [markdown]
# ## Map previous species columns

# %%
# parameters

map_previous_species_column_params = dict()

# %%
# call the task


map_previous_species_column = (
    map_column_value.set_task_instance_id("map_previous_species_column")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=convert_previous_species_string,
        col="Species",
        new_col="Species",
        mapping={
            "Bateleur": "Bateleur",
            "Buzzard_Augur": "Augur Buzzard",
            "Buzzard_Lizard": "Lizard Buzzard",
            "Chanting-goshawk_Dark": "Dark chanting goshawk",
            "Crane_Grey-crowned": "Grey crowned crane",
            "Cuckoo-hawk_African": "African cuckoo-hawk",
            "Eagle_Crowned": "Crowned eagle",
            "Eagle_Long-crested": "Long-crested eagle",
            "Eagle_Martial": "Martial eagle",
            "Eagle_Tawny": "Tawny eagle",
            "Eagle_Verreaux's": "Verreaux's eagle",
            "Eagle_Wahlberg's": "Wahlberg's eagle",
            "Eagle-owl_Spotted": "Spotted eagle owl",
            "Eagle-owl_Verreaux's": "Verreaux's eagle owl",
            "Falcon_Lanner": "Lanner falcon",
            "Falcon_Peregrine": "Peregrine falcon",
            "Fish-eagle_African": "African fish eagle",
            "Fishing-owl_Pel's": "Pel's fishing owl",
            "Goose_Egyptian": "Egyptian goose",
            "Goshawk_African": "African goshawk",
            "Goshawk_Gabar": "Gabar goshawk",
            "Harrier-hawk_African": "African harrier hawk",
            "Hawk_Bat": "Bat hawk",
            "Hawk-eagle_African": "African hawk eagle",
            "Hawk-eagle_Ayres's": "Ayre's hawk eagle",
            "Hobby_African": "African hobby",
            "Hornbill_Southern_Ground": "Southern ground hornbill",
            "Kestrel_Grey": "Grey kestrel",
            "Kestrel_Mountain": "Mountain kestrel",
            "Kite_Black": "Black kite",
            "Kite_Black-winged": "Black-winged kite",
            "Owl_African_Wood": "African wood owl",
            "Owl_Barn": "Barn owl",
            "Owl_Marsh": "Marsh owl",
            "Owl_Southern_White-faced": "Southern white-faced owl",
            "Owlet_Pearl-spotted": "Pearl-spotted owlet",
            "Pygmy-falcon_African": "African pygmy falcon",
            "Scops-owl_African": "African scops owl",
            "Secretarybird": "Secretarybird",
            "Shikra": "Shikra",
            "Snake-eagle_Black-chested": "Black-chested snake eagle",
            "Snake-eagle_Brown": "Brown snake eagle",
            "Snake-eagle_Western_Banded": "Western banded snake eagle",
            "Sparrowhawk_Black": "Black sparrowhawk",
            "Sparrowhawk_Little": "Little sparrowhawk",
            "Sparrowhawk_Ovambo": "Ovambo sparrowhawk",
            "Stork_Saddle-billed": "Saddle-billed stork",
            "Vulture_Hooded": "Hooded vulture",
            "Vulture_Lappet-faced": "Lappet-faced vulture",
            "Vulture_White-backed": "White-backed vulture",
            "Vulture_White-headed": "White-headed vulture",
            "Others": "Others",
            "Unknown": "Unknown",
        },
        **map_previous_species_column_params,
    )
    .call()
)


# %% [markdown]
# ## Split current nest_check events by group

# %%
# parameters

split_current_nest_check_params = dict()

# %%
# call the task


split_current_nest_check = (
    split_groups.set_task_instance_id("split_current_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_species_colors,
        groupers=resolved_groupers,
        **split_current_nest_check_params,
    )
    .call()
)


# %% [markdown]
# ## Split previous nest_check events by group

# %%
# parameters

split_previous_nest_check_params = dict()

# %%
# call the task


split_previous_nest_check = (
    split_groups.set_task_instance_id("split_previous_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=map_previous_species_column,
        groupers=resolved_groupers,
        **split_previous_nest_check_params,
    )
    .call()
)


# %% [markdown]
# ## Split nest_id events by group

# %%
# parameters

split_nest_id_params = dict()

# %%
# call the task


split_nest_id = (
    split_groups.set_task_instance_id("split_nest_id")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(df=restore_col_nest_id, groupers=resolved_groupers, **split_nest_id_params)
    .call()
)


# %% [markdown]
# ## Get unique conservation area

# %%
# parameters

unique_conservation_area_params = dict()

# %%
# call the task


unique_conservation_area = (
    get_first_unique_str.set_task_instance_id("unique_conservation_area")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(column_name="Conservation Name", **unique_conservation_area_params)
    .mapvalues(argnames=["df"], argvalues=split_current_nest_check)
)


# %% [markdown]
# ## Compute nest cycle status for all conservancies

# %%
# parameters

compute_nest_status_params = dict()

# %%
# call the task


compute_nest_status = (
    compute_nest_cycle_status.set_task_instance_id("compute_nest_status")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(nest_id_col="Nest ID", status_col="Status", **compute_nest_status_params)
    .mapvalues(argnames=["df"], argvalues=split_current_nest_check)
)


# %% [markdown]
# ## Zip computed nest status with nest check

# %%
# parameters

zip_compute_stat_check_params = dict()

# %%
# call the task


zip_compute_stat_check = (
    zip_groupbykey.set_task_instance_id("zip_compute_stat_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[compute_nest_status, split_current_nest_check],
        **zip_compute_stat_check_params,
    )
    .call()
)


# %% [markdown]
# ## Get active table records

# %%
# parameters

get_active_nests_params = dict()

# %%
# call the task


get_active_nests = (
    get_active_nests_table.set_task_instance_id("get_active_nests")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(nest_id_col="Nest ID", **get_active_nests_params)
    .mapvalues(argnames=["cycle_df", "original_df"], argvalues=zip_compute_stat_check)
)


# %% [markdown]
# ## Remove duplicate nests by outcome

# %%
# parameters

drop_active_duplicates_params = dict()

# %%
# call the task


drop_active_duplicates = (
    resolve_duplicate_nests_by_outcome.set_task_instance_id("drop_active_duplicates")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(nest_id_col="Nest ID", **drop_active_duplicates_params)
    .mapvalues(argnames=["df"], argvalues=get_active_nests)
)


# %% [markdown]
# ## Calculate nest building summary table

# %%
# parameters

nest_build_summary_params = dict()

# %%
# call the task


nest_build_summary = (
    build_nest_summary_table.set_task_instance_id("nest_build_summary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**nest_build_summary_params)
    .mapvalues(argnames=["df"], argvalues=drop_active_duplicates)
)


# %% [markdown]
# ## Merge names for nest building summary

# %%
# parameters

merge_nest_building_params = dict()

# %%
# call the task


merge_nest_building = (
    merge_names.set_task_instance_id("merge_nest_building")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="nest_building_summary_table", **merge_nest_building_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip nest building table with conservation area for persistence

# %%
# parameters

zip_nest_building_params = dict()

# %%
# call the task


zip_nest_building = (
    zip_groupbykey.set_task_instance_id("zip_nest_building")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[nest_build_summary, merge_nest_building], **zip_nest_building_params
    )
    .call()
)


# %% [markdown]
# ## Persist nest building table as csv

# %%
# parameters

persist_nest_building_params = dict()

# %%
# call the task


persist_nest_building = (
    persist_df_wrapper.set_task_instance_id("persist_nest_building")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["csv"],
        sanitize=False,
        **persist_nest_building_params,
    )
    .mapvalues(argnames=["df", "filename_prefix"], argvalues=zip_nest_building)
)


# %% [markdown]
# ## Parse overall df point

# %%
# parameters

parse_overall_points_params = dict()

# %%
# call the task


parse_overall_points = (
    parse_df_point.set_task_instance_id("parse_overall_points")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**parse_overall_points_params)
    .mapvalues(argnames=["df"], argvalues=split_current_nest_check)
)


# %% [markdown]
# ## Sort values by nest id on overall conservancy records

# %%
# parameters

sort_overall_nests_params = dict()

# %%
# call the task


sort_overall_nests = (
    sort_values.set_task_instance_id("sort_overall_nests")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column_name="Nest ID",
        ascending=True,
        na_position="last",
        **sort_overall_nests_params,
    )
    .mapvalues(argnames=["df"], argvalues=parse_overall_points)
)


# %% [markdown]
# ## Merge name for overall conservancy nest check

# %%
# parameters

overall_conservancy_nest_check_params = dict()

# %%
# call the task


overall_conservancy_nest_check = (
    merge_names.set_task_instance_id("overall_conservancy_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="overall_nest_check", **overall_conservancy_nest_check_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip overall nest check with conservation area for persistence

# %%
# parameters

zip_nest_check_conservation_params = dict()

# %%
# call the task


zip_nest_check_conservation = (
    zip_groupbykey.set_task_instance_id("zip_nest_check_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[sort_overall_nests, overall_conservancy_nest_check],
        **zip_nest_check_conservation_params,
    )
    .call()
)


# %% [markdown]
# ## Persist overall nest check as csv

# %%
# parameters

persist_overall_nest_check_params = dict()

# %%
# call the task


persist_overall_nest_check = (
    persist_df_wrapper.set_task_instance_id("persist_overall_nest_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["csv"],
        sanitize=False,
        **persist_overall_nest_check_params,
    )
    .mapvalues(
        argnames=["df", "filename_prefix"], argvalues=zip_nest_check_conservation
    )
)


# %% [markdown]
# ## Merge name for overall nest id check

# %%
# parameters

merge_overall_nest_id_check_params = dict()

# %%
# call the task


merge_overall_nest_id_check = (
    merge_names.set_task_instance_id("merge_overall_nest_id_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="overall_nest_id_check", **merge_overall_nest_id_check_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip overall nest id check with conservation area for persistence

# %%
# parameters

zip_nest_id_check_conservation_params = dict()

# %%
# call the task


zip_nest_id_check_conservation = (
    zip_groupbykey.set_task_instance_id("zip_nest_id_check_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[split_nest_id, merge_overall_nest_id_check],
        **zip_nest_id_check_conservation_params,
    )
    .call()
)


# %% [markdown]
# ## Persist overall nest id check as csv

# %%
# parameters

persist_overall_nest_id_check_params = dict()

# %%
# call the task


persist_overall_nest_id_check = (
    persist_df_wrapper.set_task_instance_id("persist_overall_nest_id_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["csv"],
        sanitize=False,
        **persist_overall_nest_id_check_params,
    )
    .mapvalues(
        argnames=["df", "filename_prefix"], argvalues=zip_nest_id_check_conservation
    )
)


# %% [markdown]
# ## Current period nests

# %%
# parameters

current_period_nests_params = dict()

# %%
# call the task


current_period_nests = (
    filter_by_time_range.set_task_instance_id("current_period_nests")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(time_column="time", time_range=time_range, **current_period_nests_params)
    .mapvalues(argnames=["df"], argvalues=split_nest_id)
)


# %% [markdown]
# ## Merge names for new nests

# %%
# parameters

merge_new_nests_params = dict()

# %%
# call the task


merge_new_nests = (
    merge_names.set_task_instance_id("merge_new_nests")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="new_nests", **merge_new_nests_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip new nests with conservation area for persistence

# %%
# parameters

zip_new_nest_cons_params = dict()

# %%
# call the task


zip_new_nest_cons = (
    zip_groupbykey.set_task_instance_id("zip_new_nest_cons")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[current_period_nests, merge_new_nests], **zip_new_nest_cons_params
    )
    .call()
)


# %% [markdown]
# ## Persist new nests as csv

# %%
# parameters

persist_new_nests_params = dict()

# %%
# call the task


persist_new_nests = (
    persist_df_wrapper.set_task_instance_id("persist_new_nests")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["csv"],
        sanitize=False,
        **persist_new_nests_params,
    )
    .mapvalues(argnames=["df", "filename_prefix"], argvalues=zip_new_nest_cons)
)


# %% [markdown]
# ## Aggregate nest_condition table

# %%
# parameters

nest_condition_agg_params = dict()

# %%
# call the task


nest_condition_agg = (
    aggregate_by.set_task_instance_id("nest_condition_agg")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        group_col="Condition",
        agg_col=None,
        agg_func="count",
        agg_col_name="Count",
        add_percent=False,
        sort=None,
        **nest_condition_agg_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_current_nest_check)
)


# %% [markdown]
# ## Merge names for nest condition summary

# %%
# parameters

merge_nest_condition_names_params = dict()

# %%
# call the task


merge_nest_condition_names = (
    merge_names.set_task_instance_id("merge_nest_condition_names")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="nest_condition_summary", **merge_nest_condition_names_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip nest condition summary with conservation area for persistence

# %%
# parameters

zip_nest_condition_conservation_params = dict()

# %%
# call the task


zip_nest_condition_conservation = (
    zip_groupbykey.set_task_instance_id("zip_nest_condition_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[nest_condition_agg, merge_nest_condition_names],
        **zip_nest_condition_conservation_params,
    )
    .call()
)


# %% [markdown]
# ## Persist nest condition summary

# %%
# parameters

persist_nest_condition_params = dict()

# %%
# call the task


persist_nest_condition = (
    persist_df_wrapper.set_task_instance_id("persist_nest_condition")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["csv"],
        sanitize=False,
        **persist_nest_condition_params,
    )
    .mapvalues(
        argnames=["df", "filename_prefix"], argvalues=zip_nest_condition_conservation
    )
)


# %% [markdown]
# ## Get inactive nests only

# %%
# parameters

inactive_nests_only_params = dict()

# %%
# call the task


inactive_nests_only = (
    get_inactive_nests_table.set_task_instance_id("inactive_nests_only")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(nest_id_col="Nest ID", **inactive_nests_only_params)
    .mapvalues(argnames=["cycle_df", "original_df"], argvalues=zip_compute_stat_check)
)


# %% [markdown]
# ## Resolve inactive duplicate nests

# %%
# parameters

drop_inactive_duplicates_params = dict()

# %%
# call the task


drop_inactive_duplicates = (
    resolve_duplicate_nests_by_outcome.set_task_instance_id("drop_inactive_duplicates")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(nest_id_col="Nest ID", **drop_inactive_duplicates_params)
    .mapvalues(argnames=["df"], argvalues=inactive_nests_only)
)


# %% [markdown]
# ## Normalize reported_by column

# %%
# parameters

normalize_reported_by_params = dict()

# %%
# call the task


normalize_reported_by = (
    normalize_json_column.set_task_instance_id("normalize_reported_by")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column="reported_by",
        skip_if_not_exists=True,
        sort_columns=True,
        **normalize_reported_by_params,
    )
    .mapvalues(argnames=["df"], argvalues=drop_inactive_duplicates)
)


# %% [markdown]
# ## Map columns for inactive nests

# %%
# parameters

map_inactive_nests_columns_params = dict()

# %%
# call the task


map_inactive_nests_columns = (
    map_columns.set_task_instance_id("map_inactive_nests_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        drop_columns=[],
        retain_columns=["Nest ID", "reported_by__name", "time", "geometry"],
        rename_columns={"reported_by__name": "Observer"},
        raise_if_not_found=True,
        **map_inactive_nests_columns_params,
    )
    .mapvalues(argnames=["df"], argvalues=normalize_reported_by)
)


# %% [markdown]
# ## Parse inactive df point

# %%
# parameters

parse_inactive_points_params = dict()

# %%
# call the task


parse_inactive_points = (
    parse_df_point.set_task_instance_id("parse_inactive_points")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**parse_inactive_points_params)
    .mapvalues(argnames=["df"], argvalues=map_inactive_nests_columns)
)


# %% [markdown]
# ## Map parsed inactive columns for inactive nests

# %%
# parameters

map_parsed_inactive_columns_params = dict()

# %%
# call the task


map_parsed_inactive_columns = (
    map_columns.set_task_instance_id("map_parsed_inactive_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        drop_columns=["geometry"],
        retain_columns=[],
        rename_columns={},
        raise_if_not_found=True,
        **map_parsed_inactive_columns_params,
    )
    .mapvalues(argnames=["df"], argvalues=parse_inactive_points)
)


# %% [markdown]
# ## Sort values by nest id

# %%
# parameters

sort_inactive_nests_params = dict()

# %%
# call the task


sort_inactive_nests = (
    sort_values.set_task_instance_id("sort_inactive_nests")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column_name="Nest ID",
        ascending=True,
        na_position="last",
        **sort_inactive_nests_params,
    )
    .mapvalues(argnames=["df"], argvalues=map_parsed_inactive_columns)
)


# %% [markdown]
# ## Merge names for inactive nests

# %%
# parameters

merge_inactive_nests_names_params = dict()

# %%
# call the task


merge_inactive_nests_names = (
    merge_names.set_task_instance_id("merge_inactive_nests_names")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="inactive_nests", **merge_inactive_nests_names_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip mapped inactive nests with conservation area for persistence

# %%
# parameters

zip_mapped_inactive_conservation_params = dict()

# %%
# call the task


zip_mapped_inactive_conservation = (
    zip_groupbykey.set_task_instance_id("zip_mapped_inactive_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[sort_inactive_nests, merge_inactive_nests_names],
        **zip_mapped_inactive_conservation_params,
    )
    .call()
)


# %% [markdown]
# ## Persist inactive nests as csv

# %%
# parameters

persist_inactive_nests_params = dict()

# %%
# call the task


persist_inactive_nests = (
    persist_df_wrapper.set_task_instance_id("persist_inactive_nests")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["csv"],
        sanitize=False,
        **persist_inactive_nests_params,
    )
    .mapvalues(
        argnames=["df", "filename_prefix"], argvalues=zip_mapped_inactive_conservation
    )
)


# %% [markdown]
# ## Merge names for focal species summary

# %%
# parameters

merge_focal_species_names_params = dict()

# %%
# call the task


merge_focal_species_names = (
    merge_names.set_task_instance_id("merge_focal_species_names")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="focal_species_summary", **merge_focal_species_names_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip nest check and nest id data

# %%
# parameters

zip_nest_check_id_params = dict()

# %%
# call the task


zip_nest_check_id = (
    zip_groupbykey.set_task_instance_id("zip_nest_check_id")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[split_current_nest_check, split_nest_id, merge_focal_species_names],
        **zip_nest_check_id_params,
    )
    .call()
)


# %% [markdown]
# ## Generate focal birds species summary

# %%
# parameters

focal_species_summary_params = dict()

# %%
# call the task


focal_species_summary = (
    get_focal_species_df.set_task_instance_id("focal_species_summary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        focal_birds=[
            "Augur Buzzard",
            "Tawny eagle",
            "Wahlberg's eagle",
            "Secretarybird",
            "Black-chested snake eagle",
            "Lappet-faced vulture",
            "White-backed vulture",
            "White-headed vulture",
        ],
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **focal_species_summary_params,
    )
    .mapvalues(
        argnames=["nest_check_df", "nest_id_df", "filename_prefix"],
        argvalues=zip_nest_check_id,
    )
)


# %% [markdown]
# ## Parse active df point

# %%
# parameters

parse_active_points_params = dict()

# %%
# call the task


parse_active_points = (
    parse_df_point.set_task_instance_id("parse_active_points")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**parse_active_points_params)
    .mapvalues(argnames=["df"], argvalues=drop_active_duplicates)
)


# %% [markdown]
# ## Sort values by nest id

# %%
# parameters

sort_active_nests_params = dict()

# %%
# call the task


sort_active_nests = (
    sort_values.set_task_instance_id("sort_active_nests")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column_name="Nest ID",
        ascending=True,
        na_position="last",
        **sort_active_nests_params,
    )
    .mapvalues(argnames=["df"], argvalues=parse_active_points)
)


# %% [markdown]
# ## Merge names for active nests

# %%
# parameters

merge_active_nests_names_params = dict()

# %%
# call the task


merge_active_nests_names = (
    merge_names.set_task_instance_id("merge_active_nests_names")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="active_nests", **merge_active_nests_names_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip active nests with conservation area for persistence

# %%
# parameters

zip_active_nests_conservation_params = dict()

# %%
# call the task


zip_active_nests_conservation = (
    zip_groupbykey.set_task_instance_id("zip_active_nests_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[sort_active_nests, merge_active_nests_names],
        **zip_active_nests_conservation_params,
    )
    .call()
)


# %% [markdown]
# ## Persist active nests as csv

# %%
# parameters

persist_active_nests_params = dict()

# %%
# call the task


persist_active_nests = (
    persist_df_wrapper.set_task_instance_id("persist_active_nests")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["csv"],
        sanitize=False,
        **persist_active_nests_params,
    )
    .mapvalues(
        argnames=["df", "filename_prefix"], argvalues=zip_active_nests_conservation
    )
)


# %% [markdown]
# ## Generate active nest layers

# %%
# parameters

generate_active_layers_params = dict()

# %%
# call the task


generate_active_layers = (
    create_scatterplot_layer.set_task_instance_id("generate_active_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "get_fill_color": "species_colors",
            "get_line_color": "species_colors",
            "get_radius": 2.75,
            "opacity": 0.65,
            "stroked": True,
        },
        legend={
            "title": "Active Nests",
            "label_column": "Species",
            "color_column": "species_colors",
            "sort": "ascending",
            "label_suffix": None,
        },
        **generate_active_layers_params,
    )
    .mapvalues(argnames=["geodataframe"], argvalues=drop_active_duplicates)
)


# %% [markdown]
# ## Group all conservation area layers together

# %%
# parameters

group_conservation_area_params = dict()

# %%
# call the task


group_conservation_area = (
    groupbykey.set_task_instance_id("group_conservation_area")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(iterables=[unique_conservation_area], **group_conservation_area_params)
    .call()
)


# %% [markdown]
# ## Filter polygon from kbopt gdf to draw boundaries

# %%
# parameters

filter_kbopt_conservation_params = dict()

# %%
# call the task


filter_kbopt_conservation = (
    filter_row_values.set_task_instance_id("filter_kbopt_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=filter_kbopt_aoi,
        column="Conservation Name",
        **filter_kbopt_conservation_params,
    )
    .mapvalues(argnames=["values"], argvalues=group_conservation_area)
)


# %% [markdown]
# ## Assign geom type to filtered kbopt gdf

# %%
# parameters

assign_geom_type_params = dict()

# %%
# call the task


assign_geom_type = (
    get_gdf_geom_type.set_task_instance_id("assign_geom_type")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**assign_geom_type_params)
    .mapvalues(argnames=["gdf"], argvalues=filter_kbopt_conservation)
)


# %% [markdown]
# ## Create boundary layer from conservation area

# %%
# parameters

boundary_layer_params = dict()

# %%
# call the task


boundary_layer = (
    create_deckgl_layer_from_gdf.set_task_instance_id("boundary_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        style={
            "extruded": False,
            "get_fill_color": [0, 0, 0],
            "get_line_color": [0, 0, 0],
            "get_line_width": 2.75,
            "stroked": True,
            "filled": False,
            "opacity": 0.4,
        },
        legend={
            "title": "Conservation Boundary",
            "values": [{"label": "Boundary", "color": "#000000"}],
        },
        **boundary_layer_params,
    )
    .mapvalues(argnames=["gdf"], argvalues=assign_geom_type)
)


# %% [markdown]
# ## Zip active layers with boundary layers

# %%
# parameters

zip_active_boundary_params = dict()

# %%
# call the task


zip_active_boundary = (
    zip_groupbykey.set_task_instance_id("zip_active_boundary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[generate_active_layers, boundary_layer], **zip_active_boundary_params
    )
    .call()
)


# %% [markdown]
# ## Combine styled boundary layer with active nest layers

# %%
# parameters

combine_styled_active_layers_params = dict()

# %%
# call the task


combine_styled_active_layers = (
    custom_combine_deckgl_map_layers.set_task_instance_id(
        "combine_styled_active_layers"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**combine_styled_active_layers_params)
    .mapvalues(
        argnames=["grouped_layers", "static_layers"], argvalues=zip_active_boundary
    )
)


# %% [markdown]
# ## Zoom value to conservancy boundary

# %%
# parameters

zoom_boundary_params = dict()

# %%
# call the task


zoom_boundary = (
    view_state_deck_gdf.set_task_instance_id("zoom_boundary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(pitch=0, bearing=0, **zoom_boundary_params)
    .mapvalues(argnames=["gdf"], argvalues=filter_kbopt_conservation)
)


# %% [markdown]
# ## Zip map layers with zoom value

# %%
# parameters

zip_active_zoom_params = dict()

# %%
# call the task


zip_active_zoom = (
    zip_groupbykey.set_task_instance_id("zip_active_zoom")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[combine_styled_active_layers, zoom_boundary],
        **zip_active_zoom_params,
    )
    .call()
)


# %% [markdown]
# ## Draw active species nests map

# %%
# parameters

draw_active_map_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_active_map = (
    draw_map.set_task_instance_id("draw_active_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=configure_base_maps,
        static=False,
        title=None,
        max_zoom=10,
        legend_style={"placement": "bottom-right"},
        **draw_active_map_params,
    )
    .mapvalues(argnames=["geo_layers", "view_state"], argvalues=zip_active_zoom)
)


# %% [markdown]
# ## Inject logo onto html file

# %%
# parameters

inject_active_logo_params = dict()

# %%
# call the task


inject_active_logo = (
    inject_logo_into_html.set_task_instance_id("inject_active_logo")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(logo_path=persist_kbopt_logo, **inject_active_logo_params)
    .mapvalues(argnames=["html_content"], argvalues=draw_active_map)
)


# %% [markdown]
# ## Merge conservancy name to filename

# %%
# parameters

merge_active_conservancy_params = dict()

# %%
# call the task


merge_active_conservancy = (
    merge_names.set_task_instance_id("merge_active_conservancy")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="active_nests_map.html", **merge_active_conservancy_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip filename with text for active nests

# %%
# parameters

zip_active_filename_text_params = dict()

# %%
# call the task


zip_active_filename_text = (
    zip_groupbykey.set_task_instance_id("zip_active_filename_text")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[merge_active_conservancy, inject_active_logo],
        **zip_active_filename_text_params,
    )
    .call()
)


# %% [markdown]
# ## Persist active nests map

# %%
# parameters

persist_active_map_params = dict(
    filename_suffix=...,
)

# %%
# call the task


persist_active_map = (
    persist_text.set_task_instance_id("persist_active_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        **persist_active_map_params,
    )
    .mapvalues(argnames=["filename", "text"], argvalues=zip_active_filename_text)
)


# %% [markdown]
# ## Convert active nests map html to png

# %%
# parameters

convert_active_html_png_params = dict()

# %%
# call the task


convert_active_html_png = (
    html_to_png.set_task_instance_id("convert_active_html_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **convert_active_html_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_active_map)
)


# %% [markdown]
# ## Zip nest id  previous nest check and current nest check

# %%
# parameters

zip_nest_id_curr_check_params = dict()

# %%
# call the task


zip_nest_id_curr_check = (
    zip_groupbykey.set_task_instance_id("zip_nest_id_curr_check")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[split_nest_id, split_previous_nest_check, normalize_reported_by],
        **zip_nest_id_curr_check_params,
    )
    .call()
)


# %% [markdown]
# ## Build inactive nests table

# %%
# parameters

build_inactive_table_params = dict()

# %%
# call the task


build_inactive_table = (
    build_inactive_nests_table.set_task_instance_id("build_inactive_table")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(date_col="time", **build_inactive_table_params)
    .mapvalues(
        argnames=["nest_registry_df", "overall_nest_check_df", "current_nest_check_df"],
        argvalues=zip_nest_id_curr_check,
    )
)


# %% [markdown]
# ## Parse inactive overall df point

# %%
# parameters

parse_inactive_overall_points_params = dict()

# %%
# call the task


parse_inactive_overall_points = (
    parse_df_point.set_task_instance_id("parse_inactive_overall_points")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**parse_inactive_overall_points_params)
    .mapvalues(argnames=["df"], argvalues=build_inactive_table)
)


# %% [markdown]
# ## Merge names for overall inactives

# %%
# parameters

merge_overall_inactives_params = dict()

# %%
# call the task


merge_overall_inactives = (
    merge_names.set_task_instance_id("merge_overall_inactives")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="overall_inactive_summary_table", **merge_overall_inactives_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip overall inactives with conservation area for persistence

# %%
# parameters

zip_overall_inactives_params = dict()

# %%
# call the task


zip_overall_inactives = (
    zip_groupbykey.set_task_instance_id("zip_overall_inactives")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[parse_inactive_overall_points, merge_overall_inactives],
        **zip_overall_inactives_params,
    )
    .call()
)


# %% [markdown]
# ## Persist overall inactives table as csv

# %%
# parameters

persist_overall_inactives_params = dict()

# %%
# call the task


persist_overall_inactives = (
    persist_df_wrapper.set_task_instance_id("persist_overall_inactives")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["csv"],
        sanitize=False,
        **persist_overall_inactives_params,
    )
    .mapvalues(argnames=["df", "filename_prefix"], argvalues=zip_overall_inactives)
)


# %% [markdown]
# ## Sort values by condition precedence

# %%
# parameters

sort_inactive_by_condition_params = dict()

# %%
# call the task


sort_inactive_by_condition = (
    sort_by_precedence.set_task_instance_id("sort_inactive_by_condition")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column="Condition",
        precedence={"Present": 0, "Fallen": 1, "Destroyed": 2, "Unknown": 3},
        **sort_inactive_by_condition_params,
    )
    .mapvalues(argnames=["df"], argvalues=build_inactive_table)
)


# %% [markdown]
# ## Assign Color Scheme for condition

# %%
# parameters

condition_colors_params = dict()

# %%
# call the task


condition_colors = (
    get_color_map.set_task_instance_id("condition_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        col="Condition",
        color_dict={
            "Present": "#0000ff",
            "Fallen": "#8b0000",
            "Destroyed": "#ffff00",
            "Unknown": "#a9a9a9",
        },
        **condition_colors_params,
    )
    .mapvalues(argnames=["df"], argvalues=sort_inactive_by_condition)
)


# %% [markdown]
# ## Zip condition colors and df

# %%
# parameters

zip_condition_df_params = dict()

# %%
# call the task


zip_condition_df = (
    zip_groupbykey.set_task_instance_id("zip_condition_df")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[condition_colors, sort_inactive_by_condition],
        **zip_condition_df_params,
    )
    .call()
)


# %% [markdown]
# ## Map condition colors to current events

# %%
# parameters

map_condition_colors_params = dict()

# %%
# call the task


map_condition_colors = (
    map_color_column_value.set_task_instance_id("map_condition_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        col="Condition",
        mapping=condition_colors,
        new_col="condition_colors",
        **map_condition_colors_params,
    )
    .mapvalues(argnames=["mapping", "df"], argvalues=zip_condition_df)
)


# %% [markdown]
# ## Remove null geometry from inactive

# %%
# parameters

remove_null_inactives_params = dict()

# %%
# call the task


remove_null_inactives = (
    drop_null_geometry.set_task_instance_id("remove_null_inactives")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(geometry_column="geometry", **remove_null_inactives_params)
    .mapvalues(argnames=["gdf"], argvalues=map_condition_colors)
)


# %% [markdown]
# ## Generate inactive nest layers

# %%
# parameters

generate_inactive_layers_params = dict()

# %%
# call the task


generate_inactive_layers = (
    create_scatterplot_layer.set_task_instance_id("generate_inactive_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "get_fill_color": "condition_colors",
            "get_line_color": "condition_colors",
            "get_radius": 2.75,
            "opacity": 0.65,
            "stroked": True,
        },
        legend={
            "title": "Inactive Nests",
            "label_column": "Condition",
            "color_column": "condition_colors",
            "sort": None,
        },
        **generate_inactive_layers_params,
    )
    .mapvalues(argnames=["geodataframe"], argvalues=remove_null_inactives)
)


# %% [markdown]
# ## Zip inactive layers with boundary layers

# %%
# parameters

zip_inactive_boundary_params = dict()

# %%
# call the task


zip_inactive_boundary = (
    zip_groupbykey.set_task_instance_id("zip_inactive_boundary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[generate_inactive_layers, boundary_layer],
        **zip_inactive_boundary_params,
    )
    .call()
)


# %% [markdown]
# ## Combine styled boundary layer with inactive nest layers

# %%
# parameters

combine_styled_inactive_layers_params = dict()

# %%
# call the task


combine_styled_inactive_layers = (
    custom_combine_deckgl_map_layers.set_task_instance_id(
        "combine_styled_inactive_layers"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**combine_styled_inactive_layers_params)
    .mapvalues(
        argnames=["grouped_layers", "static_layers"], argvalues=zip_inactive_boundary
    )
)


# %% [markdown]
# ## Zip map layers with zoom value

# %%
# parameters

zip_inactive_zoom_params = dict()

# %%
# call the task


zip_inactive_zoom = (
    zip_groupbykey.set_task_instance_id("zip_inactive_zoom")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[combine_styled_inactive_layers, zoom_boundary],
        **zip_inactive_zoom_params,
    )
    .call()
)


# %% [markdown]
# ## Draw inactive species nests map

# %%
# parameters

draw_inactive_map_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_inactive_map = (
    draw_map.set_task_instance_id("draw_inactive_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=configure_base_maps,
        static=False,
        title=None,
        max_zoom=10,
        legend_style={"placement": "bottom-right"},
        **draw_inactive_map_params,
    )
    .mapvalues(argnames=["geo_layers", "view_state"], argvalues=zip_inactive_zoom)
)


# %% [markdown]
# ## Inject logo onto inactive html file

# %%
# parameters

inject_inactive_logo_params = dict()

# %%
# call the task


inject_inactive_logo = (
    inject_logo_into_html.set_task_instance_id("inject_inactive_logo")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(logo_path=persist_kbopt_logo, **inject_inactive_logo_params)
    .mapvalues(argnames=["html_content"], argvalues=draw_inactive_map)
)


# %% [markdown]
# ## Merge conservancy name to filename

# %%
# parameters

merge_inactive_conservancy_params = dict()

# %%
# call the task


merge_inactive_conservancy = (
    merge_names.set_task_instance_id("merge_inactive_conservancy")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="inactive_nests_map.html", **merge_inactive_conservancy_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip filename with text for inactive nests

# %%
# parameters

zip_inactive_filename_text_params = dict()

# %%
# call the task


zip_inactive_filename_text = (
    zip_groupbykey.set_task_instance_id("zip_inactive_filename_text")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[merge_inactive_conservancy, inject_inactive_logo],
        **zip_inactive_filename_text_params,
    )
    .call()
)


# %% [markdown]
# ## Persist inactive nests map

# %%
# parameters

persist_inactive_map_params = dict(
    filename_suffix=...,
)

# %%
# call the task


persist_inactive_map = (
    persist_text.set_task_instance_id("persist_inactive_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_inactive_map_params,
    )
    .mapvalues(argnames=["filename", "text"], argvalues=zip_inactive_filename_text)
)


# %% [markdown]
# ## Convert inactive nests map html to png

# %%
# parameters

convert_inactive_html_png_params = dict()

# %%
# call the task


convert_inactive_html_png = (
    html_to_png.set_task_instance_id("convert_inactive_html_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **convert_inactive_html_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_inactive_map)
)


# %% [markdown]
# ## Set patrol and patrol events params

# %%
# parameters

er_patrol_and_events_params_params = dict()

# %%
# call the task


er_patrol_and_events_params = (
    set_patrols_and_patrol_events_params.set_task_instance_id(
        "er_patrol_and_events_params"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        client=er_client_name,
        time_range=time_range,
        include_patrol_details=True,
        raise_on_empty=False,
        truncate_to_time_range=True,
        patrols_overlap_daterange=False,
        include_null_geometry=False,
        sub_page_size=200,
        patrol_types=[],
        event_types=[],
        status=["done"],
        **er_patrol_and_events_params_params,
    )
    .call()
)


# %% [markdown]
# ## Prefetch patrols

# %%
# parameters

prefetch_patrols_params = dict()

# %%
# call the task


prefetch_patrols = (
    get_patrols_from_combined_params.set_task_instance_id("prefetch_patrols")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(combined_params=er_patrol_and_events_params, **prefetch_patrols_params)
    .call()
)


# %% [markdown]
# ## Get patrol observations

# %%
# parameters

patrol_obs_params = dict()

# %%
# call the task


patrol_obs = (
    get_patrol_observations_from_patrols_df_and_combined_params.set_task_instance_id(
        "patrol_obs"
    )
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        patrols_df=prefetch_patrols,
        combined_params=er_patrol_and_events_params,
        **patrol_obs_params,
    )
    .call()
)


# %% [markdown]
# ## Convert patrols to timezone

# %%
# parameters

convert_patrols_to_user_timezone_params = dict()

# %%
# call the task


convert_patrols_to_user_timezone = (
    convert_values_to_timezone.set_task_instance_id("convert_patrols_to_user_timezone")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=patrol_obs,
        timezone=get_timezone,
        columns=["patrol_start_time", "patrol_end_time", "fixtime"],
        **convert_patrols_to_user_timezone_params,
    )
    .call()
)


# %% [markdown]
# ##

# %%
# parameters

set_patrol_traj_color_column_params = dict()

# %%
# call the task


set_patrol_traj_color_column = (
    set_string_var.set_task_instance_id("set_patrol_traj_color_column")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(var="patrol_subject", **set_patrol_traj_color_column_params)
    .call()
)


# %% [markdown]
# ## Transform Observations to Relocations

# %%
# parameters

patrol_reloc_params = dict()

# %%
# call the task


patrol_reloc = (
    process_relocations.set_task_instance_id("patrol_reloc")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        observations=convert_patrols_to_user_timezone,
        relocs_columns=[
            "patrol_id",
            "patrol_start_time",
            "patrol_end_time",
            "patrol_type__value",
            "patrol_type__display",
            "patrol_serial_number",
            "patrol_status",
            "patrol_subject",
            "groupby_col",
            "fixtime",
            "junk_status",
            "extra__source",
            "geometry",
        ],
        filter_point_coords=[
            {"x": 180.0, "y": 90.0},
            {"x": 0.0, "y": 0.0},
            {"x": 1.0, "y": 1.0},
        ],
        **patrol_reloc_params,
    )
    .call()
)


# %% [markdown]
# ## Trajectory segment filter

# %%
# parameters

patrol_traj_params = dict(
    trajectory_segment_filter=...,
)

# %%
# call the task


patrol_traj = (
    relocations_to_trajectory.set_task_instance_id("patrol_traj")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(relocations=patrol_reloc, **patrol_traj_params)
    .call()
)


# %% [markdown]
# ## Add temporal index to patrol trajectories

# %%
# parameters

traj_add_temporal_index_params = dict()

# %%
# call the task


traj_add_temporal_index = (
    add_temporal_index.set_task_instance_id("traj_add_temporal_index")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=patrol_traj,
        time_col="extra__patrol_start_time",
        groupers=groupers,
        cast_to_datetime=True,
        format="mixed",
        **traj_add_temporal_index_params,
    )
    .call()
)


# %% [markdown]
# ## Rename value grouper columns for Trajectories

# %%
# parameters

traj_rename_grouper_columns_params = dict()

# %%
# call the task


traj_rename_grouper_columns = (
    map_columns.set_task_instance_id("traj_rename_grouper_columns")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        raise_if_not_found=True,
        df=traj_add_temporal_index,
        drop_columns=[],
        retain_columns=[],
        rename_columns={
            "extra__patrol_type__value": "patrol_type",
            "extra__patrol_serial_number": "patrol_serial_number",
            "extra__patrol_status": "patrol_status",
            "extra__patrol_subject": "patrol_subject",
            "extra__patrol_id": "patrol_id",
        },
        **traj_rename_grouper_columns_params,
    )
    .call()
)


# %% [markdown]
# ## Add spatial index to patrol trajectories

# %%
# parameters

add_spatial_index_patrol_traj_params = dict()

# %%
# call the task


add_spatial_index_patrol_traj = (
    add_spatial_index.set_task_instance_id("add_spatial_index_patrol_traj")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=traj_rename_grouper_columns,
        groupers=resolved_groupers,
        **add_spatial_index_patrol_traj_params,
    )
    .call()
)


# %% [markdown]
# ## Restore Conservation Name column for patrol trajectories

# %%
# parameters

restore_col_patrol_traj_params = dict()

# %%
# call the task


restore_col_patrol_traj = (
    add_column_from_index_level.set_task_instance_id("restore_col_patrol_traj")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        gdf=add_spatial_index_patrol_traj,
        index_name="SpatialGrouper_Conservation Name",
        column_name="Conservation Name",
        **restore_col_patrol_traj_params,
    )
    .call()
)


# %% [markdown]
# ## Persist patrols merged with kbopt gdf

# %%
# parameters

persist_patrols_kbopt_merge_params = dict()

# %%
# call the task


persist_patrols_kbopt_merge = (
    persist_df_wrapper.set_task_instance_id("persist_patrols_kbopt_merge")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename="overall_merged_patrols",
        filename_prefix=None,
        filetypes=["geoparquet"],
        sanitize=False,
        df=restore_col_patrol_traj,
        **persist_patrols_kbopt_merge_params,
    )
    .call()
)


# %% [markdown]
# ## Split patrol trajectories by conservation area

# %%
# parameters

split_patrol_traj_conservation_params = dict()

# %%
# call the task


split_patrol_traj_conservation = (
    split_groups.set_task_instance_id("split_patrol_traj_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        df=restore_col_patrol_traj,
        groupers=resolved_groupers,
        **split_patrol_traj_conservation_params,
    )
    .call()
)


# %% [markdown]
# ## Map names for patrol trajectories

# %%
# parameters

merge_patrol_traj_names_params = dict()

# %%
# call the task


merge_patrol_traj_names = (
    merge_names.set_task_instance_id("merge_patrol_traj_names")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="patrol_trajectories", **merge_patrol_traj_names_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip split patrol trajectories with conservation area for persistence

# %%
# parameters

zip_patrol_traj_conservation_params = dict()

# %%
# call the task


zip_patrol_traj_conservation = (
    zip_groupbykey.set_task_instance_id("zip_patrol_traj_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[split_patrol_traj_conservation, merge_patrol_traj_names],
        **zip_patrol_traj_conservation_params,
    )
    .call()
)


# %% [markdown]
# ## Persist patrol trajectories by conservation area

# %%
# parameters

persist_patrol_traj_conservation_params = dict()

# %%
# call the task


persist_patrol_traj_conservation = (
    persist_df_wrapper.set_task_instance_id("persist_patrol_traj_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["geoparquet"],
        sanitize=False,
        **persist_patrol_traj_conservation_params,
    )
    .mapvalues(
        argnames=["df", "filename_prefix"], argvalues=zip_patrol_traj_conservation
    )
)


# %% [markdown]
# ## Patrol trajectories colormap

# %%
# parameters

traj_colormap_params = dict()

# %%
# call the task


traj_colormap = (
    apply_color_map.set_task_instance_id("traj_colormap")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        colormap="Dark2",
        input_column_name=set_patrol_traj_color_column,
        output_column_name="patrol_traj_colormap",
        **traj_colormap_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_conservation)
)


# %% [markdown]
# ## Create patrol trajectory layers

# %%
# parameters

patrol_traj_layers_params = dict()

# %%
# call the task


patrol_traj_layers = (
    create_path_layer.set_task_instance_id("patrol_traj_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "get_color": "patrol_traj_colormap",
            "get_width": 1.85,
            "width_scale": 1,
            "width_min_pixels": 2,
            "width_max_pixels": 6,
            "width_units": "pixels",
            "cap_rounded": True,
            "joint_rounded": True,
            "billboard": False,
            "opacity": 0.55,
            "stroked": True,
        },
        legend={
            "title": "Patrol Trajectories",
            "label_column": set_patrol_traj_color_column,
            "color_column": "patrol_traj_colormap",
            "sort": "ascending",
            "label_suffix": None,
        },
        **patrol_traj_layers_params,
    )
    .mapvalues(argnames=["geodataframe"], argvalues=traj_colormap)
)


# %% [markdown]
# ## Zip patrol trajectory layers with boundary layers

# %%
# parameters

zip_patrol_traj_boundary_params = dict()

# %%
# call the task


zip_patrol_traj_boundary = (
    zip_groupbykey.set_task_instance_id("zip_patrol_traj_boundary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[patrol_traj_layers, boundary_layer],
        **zip_patrol_traj_boundary_params,
    )
    .call()
)


# %% [markdown]
# ## Combine styled boundary layer with patrol trajectory layers

# %%
# parameters

combine_patrol_traj_layers_params = dict()

# %%
# call the task


combine_patrol_traj_layers = (
    custom_combine_deckgl_map_layers.set_task_instance_id("combine_patrol_traj_layers")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**combine_patrol_traj_layers_params)
    .mapvalues(
        argnames=["grouped_layers", "static_layers"], argvalues=zip_patrol_traj_boundary
    )
)


# %% [markdown]
# ## Zoom value to patrol trajectory boundary

# %%
# parameters

zoom_patrol_traj_boundary_params = dict()

# %%
# call the task


zoom_patrol_traj_boundary = (
    view_state_deck_gdf.set_task_instance_id("zoom_patrol_traj_boundary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(pitch=0, bearing=0, **zoom_patrol_traj_boundary_params)
    .mapvalues(argnames=["gdf"], argvalues=filter_kbopt_conservation)
)


# %% [markdown]
# ## Zip map layers with zoom value

# %%
# parameters

zip_patrol_traj_zoom_params = dict()

# %%
# call the task


zip_patrol_traj_zoom = (
    zip_groupbykey.set_task_instance_id("zip_patrol_traj_zoom")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[combine_patrol_traj_layers, zoom_patrol_traj_boundary],
        **zip_patrol_traj_zoom_params,
    )
    .call()
)


# %% [markdown]
# ## Draw patrol trajectories map

# %%
# parameters

draw_patrol_traj_map_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_patrol_traj_map = (
    draw_map.set_task_instance_id("draw_patrol_traj_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=configure_base_maps,
        static=False,
        title=None,
        max_zoom=10,
        legend_style={"placement": "bottom-right"},
        **draw_patrol_traj_map_params,
    )
    .mapvalues(argnames=["geo_layers", "view_state"], argvalues=zip_patrol_traj_zoom)
)


# %% [markdown]
# ## Inject logo onto patrol html file

# %%
# parameters

inject_patrols_logo_params = dict()

# %%
# call the task


inject_patrols_logo = (
    inject_logo_into_html.set_task_instance_id("inject_patrols_logo")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(logo_path=persist_kbopt_logo, **inject_patrols_logo_params)
    .mapvalues(argnames=["html_content"], argvalues=draw_patrol_traj_map)
)


# %% [markdown]
# ## Merge conservancy name to filename

# %%
# parameters

merge_traj_conservancy_params = dict()

# %%
# call the task


merge_traj_conservancy = (
    merge_names.set_task_instance_id("merge_traj_conservancy")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="patrol_trajectories_map.html", **merge_traj_conservancy_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip filename with text for patrol trajectories map

# %%
# parameters

zip_traj_filename_text_params = dict()

# %%
# call the task


zip_traj_filename_text = (
    zip_groupbykey.set_task_instance_id("zip_traj_filename_text")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[merge_traj_conservancy, inject_patrols_logo],
        **zip_traj_filename_text_params,
    )
    .call()
)


# %% [markdown]
# ## Persist patrol trajectories map

# %%
# parameters

persist_patrol_traj_map_params = dict(
    filename_suffix=...,
)

# %%
# call the task


persist_patrol_traj_map = (
    persist_text.set_task_instance_id("persist_patrol_traj_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        **persist_patrol_traj_map_params,
    )
    .mapvalues(argnames=["filename", "text"], argvalues=zip_traj_filename_text)
)


# %% [markdown]
# ## Convert patrol trajectories map html to png

# %%
# parameters

convert_trajs_html_png_params = dict()

# %%
# call the task


convert_trajs_html_png = (
    html_to_png.set_task_instance_id("convert_trajs_html_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **convert_trajs_html_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_patrol_traj_map)
)


# %% [markdown]
# ## Zip patrol trajectories with aoi

# %%
# parameters

zip_patrol_traj_aoi_params = dict()

# %%
# call the task


zip_patrol_traj_aoi = (
    zip_groupbykey.set_task_instance_id("zip_patrol_traj_aoi")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[split_patrol_traj_conservation, filter_kbopt_conservation],
        **zip_patrol_traj_aoi_params,
    )
    .call()
)


# %% [markdown]
# ## Compute patrol visits

# %%
# parameters

patrol_grid_visits_params = dict()

# %%
# call the task


patrol_grid_visits = (
    patrol_coverage_grid.set_task_instance_id("patrol_grid_visits")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(grid_cell_size=1000, **patrol_grid_visits_params)
    .mapvalues(argnames=["trajs", "aoi"], argvalues=zip_patrol_traj_aoi)
)


# %% [markdown]
# ## Bin patrol visits with unvisited

# %%
# parameters

bin_w_unvisited_params = dict()

# %%
# call the task


bin_w_unvisited = (
    bin_with_unvisited.set_task_instance_id("bin_w_unvisited")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        column="unique_patrol_count",
        new_column="patrol_bins",
        n_bins=8,
        labels=None,
        unvisited_value=0,
        unvisited_label="unvisited",
        **bin_w_unvisited_params,
    )
    .mapvalues(argnames=["df"], argvalues=patrol_grid_visits)
)


# %% [markdown]
# ## Apply custom bin colors

# %%
# parameters

apply_patrol_bin_colors_params = dict()

# %%
# call the task


apply_patrol_bin_colors = (
    apply_bin_colors.set_task_instance_id("apply_patrol_bin_colors")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        bin_column="patrol_bins",
        new_column="patrol_bin_colors",
        unvisited_label="unvisited",
        unvisited_color="#D3D3D3",
        cmap_name="RdYlGn_r",
        **apply_patrol_bin_colors_params,
    )
    .mapvalues(argnames=["df"], argvalues=bin_w_unvisited)
)


# %% [markdown]
# ## Create patrol visits grid layer

# %%
# parameters

patrol_visits_layer_params = dict()

# %%
# call the task


patrol_visits_layer = (
    create_geojson_layer.set_task_instance_id("patrol_visits_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        layer_style={
            "filled": True,
            "stroked": True,
            "extruded": False,
            "wireframe": False,
            "get_fill_color": "colors",
            "get_line_color": [169, 169, 169],
            "opacity": 0.11,
            "get_line_width": 0.15,
            "get_elevation": 0,
            "get_point_radius": 1,
            "line_width_units": "pixels",
            "line_width_scale": 1,
            "line_width_min_pixels": 1,
            "line_width_max_pixels": 5,
        },
        legend={
            "title": "Visits per cell",
            "label_column": "patrol_bins",
            "color_column": "colors",
        },
        **patrol_visits_layer_params,
    )
    .mapvalues(argnames=["geodataframe"], argvalues=apply_patrol_bin_colors)
)


# %% [markdown]
# ## Zip patrol visits layer with boundary layer

# %%
# parameters

zip_patrol_visits_boundary_params = dict()

# %%
# call the task


zip_patrol_visits_boundary = (
    zip_groupbykey.set_task_instance_id("zip_patrol_visits_boundary")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[patrol_visits_layer, boundary_layer],
        **zip_patrol_visits_boundary_params,
    )
    .call()
)


# %% [markdown]
# ## Combine styled boundary layer with patrol visits layer

# %%
# parameters

combine_patrol_visits_layer_params = dict()

# %%
# call the task


combine_patrol_visits_layer = (
    custom_combine_deckgl_map_layers.set_task_instance_id("combine_patrol_visits_layer")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(**combine_patrol_visits_layer_params)
    .mapvalues(
        argnames=["grouped_layers", "static_layers"],
        argvalues=zip_patrol_visits_boundary,
    )
)


# %% [markdown]
# ## Zip map layers with zoom value

# %%
# parameters

zip_patrol_visits_zoom_params = dict()

# %%
# call the task


zip_patrol_visits_zoom = (
    zip_groupbykey.set_task_instance_id("zip_patrol_visits_zoom")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[combine_patrol_visits_layer, zoom_patrol_traj_boundary],
        **zip_patrol_visits_zoom_params,
    )
    .call()
)


# %% [markdown]
# ## Draw patrol visits map

# %%
# parameters

draw_patrol_visits_map_params = dict(
    widget_id=...,
)

# %%
# call the task


draw_patrol_visits_map = (
    draw_map.set_task_instance_id("draw_patrol_visits_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        tile_layers=configure_base_maps,
        static=False,
        title=None,
        max_zoom=10,
        legend_style={"placement": "bottom-right"},
        **draw_patrol_visits_map_params,
    )
    .mapvalues(argnames=["geo_layers", "view_state"], argvalues=zip_patrol_visits_zoom)
)


# %% [markdown]
# ## Inject logo onto patrol visits html file

# %%
# parameters

inject_visits_logo_params = dict()

# %%
# call the task


inject_visits_logo = (
    inject_logo_into_html.set_task_instance_id("inject_visits_logo")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(logo_path=persist_kbopt_logo, **inject_visits_logo_params)
    .mapvalues(argnames=["html_content"], argvalues=draw_patrol_visits_map)
)


# %% [markdown]
# ## Merge conservancy name to filename

# %%
# parameters

merge_visits_conservancy_params = dict()

# %%
# call the task


merge_visits_conservancy = (
    merge_names.set_task_instance_id("merge_visits_conservancy")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="patrol_visits_map.html", **merge_visits_conservancy_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip filename with text for patrol visits

# %%
# parameters

zip_visits_filename_text_params = dict()

# %%
# call the task


zip_visits_filename_text = (
    zip_groupbykey.set_task_instance_id("zip_visits_filename_text")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[merge_visits_conservancy, inject_visits_logo],
        **zip_visits_filename_text_params,
    )
    .call()
)


# %% [markdown]
# ## Persist patrol visits map

# %%
# parameters

persist_patrol_visits_map_params = dict(
    filename_suffix=...,
)

# %%
# call the task


persist_patrol_visits_map = (
    persist_text.set_task_instance_id("persist_patrol_visits_map")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        **persist_patrol_visits_map_params,
    )
    .mapvalues(argnames=["filename", "text"], argvalues=zip_visits_filename_text)
)


# %% [markdown]
# ## Convert patrol visits map html to png

# %%
# parameters

convert_visits_html_png_params = dict()

# %%
# call the task


convert_visits_html_png = (
    html_to_png.set_task_instance_id("convert_visits_html_png")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        config={
            "full_page": False,
            "device_scale_factor": 2.0,
            "wait_for_timeout": 40000,
            "max_concurrent_pages": 1,
        },
        **convert_visits_html_png_params,
    )
    .mapvalues(argnames=["html_path"], argvalues=persist_patrol_visits_map)
)


# %% [markdown]
# ## Summarize patrol efforts by ranger

# %%
# parameters

summarize_ranger_patrol_params = dict()

# %%
# call the task


summarize_ranger_patrol = (
    summarize_df.set_task_instance_id("summarize_ranger_patrol")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        groupby_cols=["patrol_subject"],
        reset_index=True,
        summary_params=[
            {
                "display_name": "no_of_patrols",
                "aggregator": "nunique",
                "column": "patrol_id",
            },
            {
                "display_name": "total_distance",
                "aggregator": "sum",
                "column": "dist_meters",
                "original_unit": "m",
                "new_unit": "km",
            },
            {
                "display_name": "total_time",
                "aggregator": "sum",
                "column": "timespan_seconds",
                "original_unit": "s",
                "new_unit": "h",
            },
        ],
        **summarize_ranger_patrol_params,
    )
    .mapvalues(argnames=["df"], argvalues=split_patrol_traj_conservation)
)


# %% [markdown]
# ## Merge conservancy name to filename

# %%
# parameters

merge_summary_conservancy_params = dict()

# %%
# call the task


merge_summary_conservancy = (
    merge_names.set_task_instance_id("merge_summary_conservancy")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="patrol_efforts_summary", **merge_summary_conservancy_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip patrol efforts summary with conservation area for persistence

# %%
# parameters

zip_patrol_effort_conservation_params = dict()

# %%
# call the task


zip_patrol_effort_conservation = (
    zip_groupbykey.set_task_instance_id("zip_patrol_effort_conservation")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[summarize_ranger_patrol, merge_summary_conservancy],
        **zip_patrol_effort_conservation_params,
    )
    .call()
)


# %% [markdown]
# ## Persist patrol efforts summary

# %%
# parameters

persist_patrol_effort_params = dict()

# %%
# call the task


persist_patrol_effort = (
    persist_df_wrapper.set_task_instance_id("persist_patrol_effort")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        root_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        filename=None,
        filetypes=["csv"],
        sanitize=False,
        **persist_patrol_effort_params,
    )
    .mapvalues(
        argnames=["df", "filename_prefix"], argvalues=zip_patrol_effort_conservation
    )
)


# %% [markdown]
# ## Merge conservancy name and docx to persist filename

# %%
# parameters

merge_docx_names_params = dict()

# %%
# call the task


merge_docx_names = (
    merge_names.set_task_instance_id("merge_docx_names")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(b="overall_report.docx", **merge_docx_names_params)
    .mapvalues(argnames=["a"], argvalues=unique_conservation_area)
)


# %% [markdown]
# ## Zip conservancy name with docx name

# %%
# parameters

conservancy_docx_params = dict()

# %%
# call the task


conservancy_docx = (
    zip_groupbykey.set_task_instance_id("conservancy_docx")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        sequences=[unique_conservation_area, merge_docx_names],
        **conservancy_docx_params,
    )
    .call()
)


# %% [markdown]
# ## Download KBOPT word template

# %%
# parameters

persist_kbopt_word_params = dict()

# %%
# call the task


persist_kbopt_word = (
    fetch_and_persist_file.set_task_instance_id("persist_kbopt_word")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        url="https://www.dropbox.com/scl/fi/455zp265vce21052hvmiq/Nest-Monitoring-Report-Template.docx?rlkey=09y3qf08u9t0jhonwxfurc027&st=0cxqjlyu&dl=0",
        output_path=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        overwrite_existing=False,
        retries=3,
        unzip=False,
        **persist_kbopt_word_params,
    )
    .call()
)


# %% [markdown]
# ## Generate word docx for all conservancies

# %%
# parameters

word_docx_output_params = dict()

# %%
# call the task


word_docx_output = (
    generate_nest_report.set_task_instance_id("word_docx_output")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        output_dir=os.environ["ECOSCOPE_WORKFLOWS_RESULTS"],
        template_path=persist_kbopt_word,
        time_period=time_range,
        **word_docx_output_params,
    )
    .mapvalues(argnames=["conservancy_name", "filename"], argvalues=conservancy_docx)
)


# %% [markdown]
# ## Overall dashboard

# %%
# parameters

overall_dashboard_params = dict(
    warning=...,
)

# %%
# call the task


overall_dashboard = (
    gather_dashboard.set_task_instance_id("overall_dashboard")
    .handle_errors()
    .with_tracing()
    .skipif(
        conditions=[
            any_is_empty_df,
            any_dependency_skipped,
        ],
        unpack_depth=1,
    )
    .partial(
        details=workflow_details,
        widgets=[],
        time_range=time_range,
        groupers=resolved_groupers,
        **overall_dashboard_params,
    )
    .call()
)
